# Public frontend configuration only. Leave blank for same-origin Render deployment.
VITE_API_BASE_URL=

# For separate local backend only:
# VITE_API_BASE_URL=http://127.0.0.1:8000

# Never add access tokens, Bybit secrets, Supabase service-role keys, or other credentials here.
