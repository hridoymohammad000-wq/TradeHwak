export enum ActivePage {
  DASHBOARD = 'dashboard',
  SCANNER = 'scanner',
  SIGNALS = 'signals',
  CHART = 'chart',
  ACTIVE_TRADES = 'active_trades',
  JOURNAL = 'journal',
  SETTINGS = 'settings',
}

export type { RuntimeMode, TradingMode } from './api/types';
