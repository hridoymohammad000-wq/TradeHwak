# Same-origin production deployment: leave VITE_API_BASE_URL unset.
# Set it only for a separate local or remote backend.

# Never add access tokens, Bybit secrets, Supabase service-role keys, or other credentials here.
